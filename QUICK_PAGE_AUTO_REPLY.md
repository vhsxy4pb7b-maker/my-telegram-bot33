# 快速配置页面自动回复

## 🚀 快速开始

### 使用配置工具（最简单）

```bash
python configure_page_auto_reply.py
```

然后按照提示操作：
1. 选择 `2` 添加/更新页面配置
2. 输入页面ID
3. 输入页面名称（可选）
4. 选择是否启用自动回复

### 手动配置

编辑 `config.yaml` 文件，添加：

```yaml
page_settings:
  "你的页面ID":
    auto_reply_enabled: true
    name: "页面名称"
```

## 📋 配置示例

### 启用特定页面的自动回复

```yaml
auto_reply:
  enabled: false  # 全局禁用

page_settings:
  "1234567890123456":
    auto_reply_enabled: true  # 只对这个页面启用
    name: "主要业务页面"
```

### 禁用特定页面的自动回复

```yaml
auto_reply:
  enabled: true  # 全局启用

page_settings:
  "9876543210987654":
    auto_reply_enabled: false  # 只对这个页面禁用
    name: "测试页面"
```

## 🔍 如何获取页面ID

1. **从日志中查看** - 收到消息时，日志会显示 `page_id`
2. **使用配置工具** - 运行工具后选择"检查页面自动回复状态"
3. **从Facebook API获取** - 调用 `/me/accounts` 接口

## ✅ 验证配置

运行配置工具，选择 `4` 检查页面状态，输入页面ID即可查看当前配置。

## 📚 更多信息

详细文档请查看 [PAGE_AUTO_REPLY_GUIDE.md](PAGE_AUTO_REPLY_GUIDE.md)


